import React from "react";
import { IconPropsModel } from "../models/icon.model";

export class HadesIcon extends React.Component<IconPropsModel> {
  constructor(props = new IconPropsModel()) {
    super(props);
  }
}
