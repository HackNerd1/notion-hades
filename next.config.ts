import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["static.licdn.com", "static.cdninstagram.com", "github.com", "www.notion.so", "prod-files-secure.s3.us-west-2.amazonaws.com", "developer.mozilla.org", "github.githubassets.com"],
  },
};

export default nextConfig;
